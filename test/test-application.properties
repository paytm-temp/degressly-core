# Mock server configurations
primary.host=http://localhost:9000
secondary.host=http://localhost:9001
candidate.host=http://localhost:9002

# Default configuration (will be overridden by environment variables)
wait.after.forwarding.to.primary=0

# Enable debug logging
logging.level.com.degressly=DEBUG
debug=true

# Return response from primary by default
return.response.from=PRIMARY
